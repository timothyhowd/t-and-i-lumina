# Raw .docx templates and extracted text contain real legal prose.
# Per POC-LIMITATIONS.md option (ii), do not commit these.
# Only the slot schemas in corpus/templates/ are committed.
raw/
extracts/

# Per-machine feedback log. Each runs.jsonl accumulates user observations
# tied to drafts they generated. May contain test prompts with realistic
# (mock-persona) employee data; keep local. The README + schema are
# committed so the format is shared.
feedback/runs.jsonl
feedback/*.local.jsonl
